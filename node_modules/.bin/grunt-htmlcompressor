#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-htmlcompressor/bin/grunt-htmlcompressor" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-htmlcompressor/bin/grunt-htmlcompressor" "$@"
  ret=$?
fi
exit $ret
